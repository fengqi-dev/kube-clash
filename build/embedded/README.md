Release builds place the platform-specific `kubeloop-helper` binary here before
building the desktop application. The desktop binary embeds it and materializes
it under `~/.kubeloop/helper/` when needed.
